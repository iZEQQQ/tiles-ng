import { PostRatingRequest } from './post-rating-request';

describe('PostRatingRequest', () => {
  it('should create an instance', () => {
    expect(new PostRatingRequest()).toBeTruthy();
  });
});
